<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_bg"
    android:clickable="true"
    android:focusable="true"
    android:padding="18dp">

    <!-- Top Row: App Icon & Sentry Status Badge -->
    <ImageView
        android:id="@+id/widget_logo"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_widget_logo" />

    <TextView
        android:id="@+id/widget_mode_badge"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/widget_btn_off"
        android:gravity="center"
        android:paddingHorizontal="10dp"
        android:text="Reading Mask"
        android:textColor="#8E8E93"
        android:textSize="10sp"
        android:textStyle="bold" />

    <!-- Center / Bottom Content: Title, Subtitle, and Full-Width Action Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/widget_action_button"
        android:layout_marginBottom="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_app_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/widget_status_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="sans-serif"
            android:text="Protection Inactive"
            android:textColor="#8E8E93"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_action_button"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/widget_btn_off"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:text="Turn On Privacy"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold" />

</RelativeLayout>
